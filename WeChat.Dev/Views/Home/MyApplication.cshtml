
@{
    ViewBag.Title = "我的发起";
    Layout = "~/Views/Shared/_ListLayout.cshtml";
}
<div class="list" id="myapplication"></div>
@section scripts{
    <script type="text/javascript">
        try {
            init();
        } catch (e) {
            console.error(e)
        }

        function init() {
            axios
                .get('/Scripts/data/myapplication.data.json')
                .then(function (response) {
                    try {
                        var data = response.data;
                        data.listCategory = 1;//发起
                        var html = template('tpl_listtemplate', data);
                        document.getElementById('myapplication').innerHTML = html;
                        utils.setTitle("我的发起", data.items.length);
                        bindJumpLink();
                    } catch (e) {
                        console.error(e)
                    }
                })
                .catch(function (err) {
                    weui.alert(JSON.stringify(err));
                });
        }
    </script>
}